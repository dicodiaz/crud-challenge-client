import { FC } from 'react';
import { Row } from 'react-bootstrap';

const Home: FC = () => (
  <Row className="py-5">
    <h1 className="display-1 text-white text-center">Welcome!</h1>
  </Row>
);

export default Home;
